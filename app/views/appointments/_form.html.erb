<div class="appointment-container">
  <%= form_with model: @appointment, local: true, data: { controller: "calendar appointment", appointment_nothing_selected_value: t("appointments.nothing_selected") } do |f| %>
  <div class="appointment-header">
    <div class="appointment-header-left">
      <%= link_to calendar_appointments_path, class: "back-link inline" do %>
        <%= image_tag "back.png",
              class: "wiz-icon",
              width: 24,
              height: 24 %>
      <% end %>

      <h2>
        <%= @appointment.new_record? ?
            t("appointments.new") :
            t("appointments.edit") %>
      </h2>
    </div>

    <button type="submit" class="wizard-save-button"
            data-appointment-target="saveButton">
        <%= t("common.save").upcase %>
    </button>
  </div>

    <!-- Client Name -->
    <div class="client-input-wrapper">
      <label>
        <%= t("appointments.form.client_name") %>
        <span class="required-asterisk">*</span>
      </label>
      <%= text_field_tag "appointment[client_name]",
        @appointment.client&.full_name || "",
        class: "form-input client-name-input",
        placeholder: t("appointments.form.client_name_placeholder"),
        data: {
          "calendar-target": "clientName",
          action: "input->calendar#updateClientInfo keyup->calendar#toggleClearButton focus->calendar#toggleClearButton"
        },
        list: "client-list",
        autocomplete: "off" %>

      <% @appointment.errors.full_messages_for(:client_name).each do |message| %>
        <div class="inline-error">
          <%= message %>
        </div>
      <% end %>

      <button type="button"
              class="clear-client-btn hidden"
              data-calendar-target="clearButton"
              data-action="click->calendar#clearClientField">
        <%= image_tag "close.png", class: "clear-icon" %>
      </button>

      <datalist id="client-list" data-calendar-target="clientList">
        <% current_user.clients.each do |client| %>
          <option value="<%= client.full_name %>" data-phone="<%= client.phone %>">
        <% end %>
      </datalist>
    </div>

    <!-- Phone -->
    <div class="form-section">
      <label>
        <%= t("appointments.form.phone") %>
        <span class="required-asterisk">*</span>
      </label>

      <%= telephone_field_tag "appointment[phone]",
        @appointment.client&.phone || "",
        class: "form-input",
        data: {
          controller: "phone-mask",
          "calendar-target": "clientPhone"
        } %>

      <% @appointment.errors.full_messages_for(:phone).each do |message| %>
        <div class="inline-error">
          <%= message %>
        </div>
      <% end %>
    </div>

    <!-- Date -->
    <div class="form-section">
      <label>
        <%= t("appointments.form.date") %>
        <span class="required-asterisk">*</span>
      </label>

      <%= text_field_tag "appointment[appointment_date]",
        (@appointment.appointment_date || Date.today).strftime("%d.%m.%Y"),
        class: "form-input",
        readonly: true,
        id: "appointment_date",
        data: {
          controller: "datepicker",
          datepicker_target: "input",
          min: Date.today.strftime("%Y-%m-%d")
        } %>

      <% @appointment.errors.full_messages_for(:appointment_date).each do |message| %>
        <div class="inline-error">
          <%= message %>
        </div>
      <% end %>
    </div>

    <!-- Start / End Time -->
    <div class="form-section double">
      <div>
        <%= f.label :appointment_time, t("appointments.form.start_time") %>
        <span class="required-asterisk">*</span>
        <%= f.time_field :appointment_time,
          step: 300,
          class: "form-input",
          data: {
            appointment_target: "appointmentTime",
            action: "blur->appointment#roundToNearestFive input->appointment#validateTimes change->appointment#validateTimes"
          } %>

        <% @appointment.errors.full_messages_for(:appointment_time).each do |message| %>
          <div class="inline-error">
            <%= message %>
          </div>
        <% end %>
      </div>

      <div>
        <%= f.label :end_time, t("appointments.form.end_time") %>
        <span class="required-asterisk">*</span>
        <%= f.time_field :end_time,
          step: 300,
          class: "form-input",
          data: {
            appointment_target: "endTime",
            action: "blur->appointment#roundToNearestFive input->appointment#validateTimes change->appointment#validateTimes"
          } %>

        <% @appointment.errors.full_messages_for(:end_time).each do |message| %>
          <div class="inline-error">
            <%= message %>
          </div>
        <% end %>
        <div data-appointment-target="timeError"
            class="inline-error hidden">
          <%= t("appointments.form.end_time_error", default: "Час завершення має бути пізніше часу початку") %>
        </div>
      </div>
    </div>

    <!-- Main Services -->
    <div class="form-section">
      <label>
        <%= t("appointments.form.services") %>
      </label>

      <div class="service-selector">
        <% if @appointment.service_ids.any? %>
          <div class="service-selector-content"
              data-action="click->appointment#open"
              data-type="service">

            <div data-appointment-target="serviceSelected">
              <%= @appointment.services.map(&:name).join(", ") %>
            </div>

          </div>
        <% else %>
          <div class="collapse-section add-service-placeholder">
            <button type="button"
                    class="collapse-toggle"
                    data-action="click->appointment#open"
                    data-type="service">
              <span>
                <%= t("appointments.form.add_services") %>
              </span>
              <%= image_tag "plus.png",
                    class: "add-icon" %>
            </button>
          </div>

          <div data-appointment-target="serviceSelected"
              class="hidden">
          </div>
        <% end %>
      </div>
    </div>

    <!-- Hidden combined IDs -->
    <input type="hidden"
           data-appointment-target="hiddenInput"
           value="<%= @appointment.service_ids.join(',') %>">

    <!-- Notes -->
    <div class="form-section">
      <%= f.label :notes, t("appointments.form.notes") %>
      <%= f.text_area :notes,
        class: "form-input textarea-auto",
        maxlength: 256,
        rows: 2,
        placeholder: t("appointments.form.notes_placeholder") %>
    </div>

    <!-- Modals -->
    <%= render "appointments/modals/service_modal" %>
  <% end %>
</div>
